import { Stack } from '@chakra-ui/react'

export const Main = (props) => (
  <Stack
    height="100vh"
    width="100%"
    maxWidth="48rem"
    margin="0"
    padding="0"
    spacing="1.5rem"
    justifyContent="space-evenly"
    {...props}
  />
)
